# 后端地址。生产走同域反代时留空。
VITE_API_BASE=http://localhost:18080

# ---- 仅本地调试：让 dev server 现场签发 initData ----
# 不带 VITE_ 前缀 = 只有 vite.config 的 Node 侧读得到，不会进前端 bundle。
# 值要与后端库里 bot.token_enc 存的那把一致。
DEV_BOT_TOKEN=123456:AA-demo-bot-token
DEV_TRACKING_ID=aB3xY9zK1m
# DEV_TG_USER_ID=424242
