# Shared packages

Reserved for libraries shared by more than one app (API clients, design tokens, etc.).

Do not add a package until a second consumer exists — premature extraction costs more than duplication at this stage.
