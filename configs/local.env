# Local development secrets and connection strings. Gitignored — do not commit.
#   source configs/local.env
#
# Connects to the shared Supabase ignition schema as ignition_app (non-privileged,
# no BYPASSRLS) — RLS only applies with this role. Do not switch back to the postgres role.
export IGNITION_PG_DSN='postgresql://ignition_app.pjnoxuicuchdqbmstwmp:wLlJzfRPe7zi53OkGrdFfeANonHPwRXB2eqDEdJj@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require'

# Must match the key used when sealing bot.token_enc / api_key.secret_enc in the database.
# A new key will not decrypt existing ciphertext (symptom: service starts, but TMA session returns 500).
export IGNITION_MASTER_KEY='GuOigr19n2Jg4lPWrp7FP7eniU2v/VcpBvD3CIJ2OwI='
export IGNITION_JWT_KEY='dev-jwt-signing-key-not-for-prod'
